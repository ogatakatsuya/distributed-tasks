apiVersion: batch/v1
kind: Job
metadata:
  name: producer
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: producer
        image: asia-northeast1-docker.pkg.dev/distributed-task-471604/distributed-task-repo/kafka-producer:latest
        imagePullPolicy: Always
